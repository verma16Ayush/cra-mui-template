import { createTheme } from "@mui/material/styles";

export const THEME = createTheme({
  palette: {
    common: {
      white: '#FEFBFB',
      black: '#000000'
    },
    primary: {
      main: '#E05E57'
    },
    secondary: {
      main: '#FBEEEE'
    }
  }
});